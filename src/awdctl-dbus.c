/*
 * Generated by gdbus-codegen 2.60.5 from fr.mpostaire.awdctl.xml. DO NOT EDIT.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "awdctl-dbus.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface fr.mpostaire.awdctl.Brightness
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:AwdctlBrightness
 * @title: AwdctlBrightness
 * @short_description: Generated C code for the fr.mpostaire.awdctl.Brightness D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link> D-Bus interface in C.
 */

/* ---- Introspection data for fr.mpostaire.awdctl.Brightness ---- */

static const _ExtendedGDBusArgInfo _awdctl_brightness_method_info_set_brightness_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _awdctl_brightness_method_info_set_brightness_IN_ARG_pointers[] =
{
  &_awdctl_brightness_method_info_set_brightness_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _awdctl_brightness_method_info_set_brightness =
{
  {
    -1,
    (gchar *) "SetBrightness",
    (GDBusArgInfo **) &_awdctl_brightness_method_info_set_brightness_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-brightness",
  FALSE
};

static const _ExtendedGDBusArgInfo _awdctl_brightness_method_info_inc_brightness_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _awdctl_brightness_method_info_inc_brightness_IN_ARG_pointers[] =
{
  &_awdctl_brightness_method_info_inc_brightness_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _awdctl_brightness_method_info_inc_brightness =
{
  {
    -1,
    (gchar *) "IncBrightness",
    (GDBusArgInfo **) &_awdctl_brightness_method_info_inc_brightness_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-inc-brightness",
  FALSE
};

static const _ExtendedGDBusArgInfo _awdctl_brightness_method_info_dec_brightness_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _awdctl_brightness_method_info_dec_brightness_IN_ARG_pointers[] =
{
  &_awdctl_brightness_method_info_dec_brightness_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _awdctl_brightness_method_info_dec_brightness =
{
  {
    -1,
    (gchar *) "DecBrightness",
    (GDBusArgInfo **) &_awdctl_brightness_method_info_dec_brightness_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-dec-brightness",
  FALSE
};

static const GDBusMethodInfo * const _awdctl_brightness_method_info_pointers[] =
{
  &_awdctl_brightness_method_info_set_brightness.parent_struct,
  &_awdctl_brightness_method_info_inc_brightness.parent_struct,
  &_awdctl_brightness_method_info_dec_brightness.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _awdctl_brightness_property_info_percentage =
{
  {
    -1,
    (gchar *) "Percentage",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "percentage",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _awdctl_brightness_property_info_min_percentage =
{
  {
    -1,
    (gchar *) "MinPercentage",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "min-percentage",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _awdctl_brightness_property_info_pointers[] =
{
  &_awdctl_brightness_property_info_percentage.parent_struct,
  &_awdctl_brightness_property_info_min_percentage.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _awdctl_brightness_interface_info =
{
  {
    -1,
    (gchar *) "fr.mpostaire.awdctl.Brightness",
    (GDBusMethodInfo **) &_awdctl_brightness_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_awdctl_brightness_property_info_pointers,
    NULL
  },
  "awdctl-brightness",
};


/**
 * awdctl_brightness_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
awdctl_brightness_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_awdctl_brightness_interface_info.parent_struct;
}

/**
 * awdctl_brightness_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #AwdctlBrightness interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
awdctl_brightness_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "percentage");
  g_object_class_override_property (klass, property_id_begin++, "min-percentage");
  return property_id_begin - 1;
}



/**
 * AwdctlBrightness:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link>.
 */

/**
 * AwdctlBrightnessIface:
 * @parent_iface: The parent interface.
 * @handle_dec_brightness: Handler for the #AwdctlBrightness::handle-dec-brightness signal.
 * @handle_inc_brightness: Handler for the #AwdctlBrightness::handle-inc-brightness signal.
 * @handle_set_brightness: Handler for the #AwdctlBrightness::handle-set-brightness signal.
 * @get_min_percentage: Getter for the #AwdctlBrightness:min-percentage property.
 * @get_percentage: Getter for the #AwdctlBrightness:percentage property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link>.
 */

typedef AwdctlBrightnessIface AwdctlBrightnessInterface;
G_DEFINE_INTERFACE (AwdctlBrightness, awdctl_brightness, G_TYPE_OBJECT)

static void
awdctl_brightness_default_init (AwdctlBrightnessIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * AwdctlBrightness::handle-set-brightness:
   * @object: A #AwdctlBrightness.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.SetBrightness">SetBrightness()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_brightness_complete_set_brightness() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-brightness",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlBrightnessIface, handle_set_brightness),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * AwdctlBrightness::handle-inc-brightness:
   * @object: A #AwdctlBrightness.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.IncBrightness">IncBrightness()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_brightness_complete_inc_brightness() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-inc-brightness",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlBrightnessIface, handle_inc_brightness),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * AwdctlBrightness::handle-dec-brightness:
   * @object: A #AwdctlBrightness.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.DecBrightness">DecBrightness()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_brightness_complete_dec_brightness() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dec-brightness",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlBrightnessIface, handle_dec_brightness),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /* GObject properties for D-Bus properties: */
  /**
   * AwdctlBrightness:percentage:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fr-mpostaire-awdctl-Brightness.Percentage">"Percentage"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("percentage", "Percentage", "Percentage", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * AwdctlBrightness:min-percentage:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fr-mpostaire-awdctl-Brightness.MinPercentage">"MinPercentage"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("min-percentage", "MinPercentage", "MinPercentage", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * awdctl_brightness_get_percentage: (skip)
 * @object: A #AwdctlBrightness.
 *
 * Gets the value of the <link linkend="gdbus-property-fr-mpostaire-awdctl-Brightness.Percentage">"Percentage"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
awdctl_brightness_get_percentage (AwdctlBrightness *object)
{
  return AWDCTL_BRIGHTNESS_GET_IFACE (object)->get_percentage (object);
}

/**
 * awdctl_brightness_set_percentage: (skip)
 * @object: A #AwdctlBrightness.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fr-mpostaire-awdctl-Brightness.Percentage">"Percentage"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
awdctl_brightness_set_percentage (AwdctlBrightness *object, guint value)
{
  g_object_set (G_OBJECT (object), "percentage", value, NULL);
}

/**
 * awdctl_brightness_get_min_percentage: (skip)
 * @object: A #AwdctlBrightness.
 *
 * Gets the value of the <link linkend="gdbus-property-fr-mpostaire-awdctl-Brightness.MinPercentage">"MinPercentage"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
awdctl_brightness_get_min_percentage (AwdctlBrightness *object)
{
  return AWDCTL_BRIGHTNESS_GET_IFACE (object)->get_min_percentage (object);
}

/**
 * awdctl_brightness_set_min_percentage: (skip)
 * @object: A #AwdctlBrightness.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fr-mpostaire-awdctl-Brightness.MinPercentage">"MinPercentage"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
awdctl_brightness_set_min_percentage (AwdctlBrightness *object, guint value)
{
  g_object_set (G_OBJECT (object), "min-percentage", value, NULL);
}

/**
 * awdctl_brightness_call_set_brightness:
 * @proxy: A #AwdctlBrightnessProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.SetBrightness">SetBrightness()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_brightness_call_set_brightness_finish() to get the result of the operation.
 *
 * See awdctl_brightness_call_set_brightness_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_brightness_call_set_brightness (
    AwdctlBrightness *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetBrightness",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_brightness_call_set_brightness_finish:
 * @proxy: A #AwdctlBrightnessProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_brightness_call_set_brightness().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_brightness_call_set_brightness().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_brightness_call_set_brightness_finish (
    AwdctlBrightness *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_brightness_call_set_brightness_sync:
 * @proxy: A #AwdctlBrightnessProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.SetBrightness">SetBrightness()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_brightness_call_set_brightness() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_brightness_call_set_brightness_sync (
    AwdctlBrightness *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetBrightness",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_brightness_call_inc_brightness:
 * @proxy: A #AwdctlBrightnessProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.IncBrightness">IncBrightness()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_brightness_call_inc_brightness_finish() to get the result of the operation.
 *
 * See awdctl_brightness_call_inc_brightness_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_brightness_call_inc_brightness (
    AwdctlBrightness *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "IncBrightness",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_brightness_call_inc_brightness_finish:
 * @proxy: A #AwdctlBrightnessProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_brightness_call_inc_brightness().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_brightness_call_inc_brightness().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_brightness_call_inc_brightness_finish (
    AwdctlBrightness *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_brightness_call_inc_brightness_sync:
 * @proxy: A #AwdctlBrightnessProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.IncBrightness">IncBrightness()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_brightness_call_inc_brightness() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_brightness_call_inc_brightness_sync (
    AwdctlBrightness *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "IncBrightness",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_brightness_call_dec_brightness:
 * @proxy: A #AwdctlBrightnessProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.DecBrightness">DecBrightness()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_brightness_call_dec_brightness_finish() to get the result of the operation.
 *
 * See awdctl_brightness_call_dec_brightness_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_brightness_call_dec_brightness (
    AwdctlBrightness *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DecBrightness",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_brightness_call_dec_brightness_finish:
 * @proxy: A #AwdctlBrightnessProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_brightness_call_dec_brightness().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_brightness_call_dec_brightness().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_brightness_call_dec_brightness_finish (
    AwdctlBrightness *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_brightness_call_dec_brightness_sync:
 * @proxy: A #AwdctlBrightnessProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.DecBrightness">DecBrightness()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_brightness_call_dec_brightness() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_brightness_call_dec_brightness_sync (
    AwdctlBrightness *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DecBrightness",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_brightness_complete_set_brightness:
 * @object: A #AwdctlBrightness.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.SetBrightness">SetBrightness()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_brightness_complete_set_brightness (
    AwdctlBrightness *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * awdctl_brightness_complete_inc_brightness:
 * @object: A #AwdctlBrightness.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.IncBrightness">IncBrightness()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_brightness_complete_inc_brightness (
    AwdctlBrightness *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * awdctl_brightness_complete_dec_brightness:
 * @object: A #AwdctlBrightness.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Brightness.DecBrightness">DecBrightness()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_brightness_complete_dec_brightness (
    AwdctlBrightness *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * AwdctlBrightnessProxy:
 *
 * The #AwdctlBrightnessProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * AwdctlBrightnessProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #AwdctlBrightnessProxy.
 */

struct _AwdctlBrightnessProxyPrivate
{
  GData *qdata;
};

static void awdctl_brightness_proxy_iface_init (AwdctlBrightnessIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (AwdctlBrightnessProxy, awdctl_brightness_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (AwdctlBrightnessProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_BRIGHTNESS, awdctl_brightness_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (AwdctlBrightnessProxy, awdctl_brightness_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_BRIGHTNESS, awdctl_brightness_proxy_iface_init))

#endif
static void
awdctl_brightness_proxy_finalize (GObject *object)
{
  AwdctlBrightnessProxy *proxy = AWDCTL_BRIGHTNESS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (awdctl_brightness_proxy_parent_class)->finalize (object);
}

static void
awdctl_brightness_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _awdctl_brightness_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
awdctl_brightness_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface fr.mpostaire.awdctl.Brightness: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
awdctl_brightness_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _awdctl_brightness_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "fr.mpostaire.awdctl.Brightness", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) awdctl_brightness_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
awdctl_brightness_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_awdctl_brightness_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_AWDCTL_BRIGHTNESS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_AWDCTL_BRIGHTNESS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
awdctl_brightness_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  AwdctlBrightnessProxy *proxy = AWDCTL_BRIGHTNESS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_brightness_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_brightness_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static guint 
awdctl_brightness_proxy_get_percentage (AwdctlBrightness *object)
{
  AwdctlBrightnessProxy *proxy = AWDCTL_BRIGHTNESS_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Percentage");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
awdctl_brightness_proxy_get_min_percentage (AwdctlBrightness *object)
{
  AwdctlBrightnessProxy *proxy = AWDCTL_BRIGHTNESS_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MinPercentage");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
awdctl_brightness_proxy_init (AwdctlBrightnessProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = awdctl_brightness_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_AWDCTL_BRIGHTNESS_PROXY, AwdctlBrightnessProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), awdctl_brightness_interface_info ());
}

static void
awdctl_brightness_proxy_class_init (AwdctlBrightnessProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = awdctl_brightness_proxy_finalize;
  gobject_class->get_property = awdctl_brightness_proxy_get_property;
  gobject_class->set_property = awdctl_brightness_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = awdctl_brightness_proxy_g_signal;
  proxy_class->g_properties_changed = awdctl_brightness_proxy_g_properties_changed;

  awdctl_brightness_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (AwdctlBrightnessProxyPrivate));
#endif
}

static void
awdctl_brightness_proxy_iface_init (AwdctlBrightnessIface *iface)
{
  iface->get_percentage = awdctl_brightness_proxy_get_percentage;
  iface->get_min_percentage = awdctl_brightness_proxy_get_min_percentage;
}

/**
 * awdctl_brightness_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_brightness_proxy_new_finish() to get the result of the operation.
 *
 * See awdctl_brightness_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
awdctl_brightness_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_AWDCTL_BRIGHTNESS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Brightness", NULL);
}

/**
 * awdctl_brightness_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_brightness_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with awdctl_brightness_proxy_new().
 *
 * Returns: (transfer full) (type AwdctlBrightnessProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlBrightness *
awdctl_brightness_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return AWDCTL_BRIGHTNESS (ret);
  else
    return NULL;
}

/**
 * awdctl_brightness_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See awdctl_brightness_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type AwdctlBrightnessProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlBrightness *
awdctl_brightness_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_AWDCTL_BRIGHTNESS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Brightness", NULL);
  if (ret != NULL)
    return AWDCTL_BRIGHTNESS (ret);
  else
    return NULL;
}


/**
 * awdctl_brightness_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like awdctl_brightness_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_brightness_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See awdctl_brightness_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
awdctl_brightness_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_AWDCTL_BRIGHTNESS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Brightness", NULL);
}

/**
 * awdctl_brightness_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_brightness_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with awdctl_brightness_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type AwdctlBrightnessProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlBrightness *
awdctl_brightness_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return AWDCTL_BRIGHTNESS (ret);
  else
    return NULL;
}

/**
 * awdctl_brightness_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like awdctl_brightness_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See awdctl_brightness_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type AwdctlBrightnessProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlBrightness *
awdctl_brightness_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_AWDCTL_BRIGHTNESS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Brightness", NULL);
  if (ret != NULL)
    return AWDCTL_BRIGHTNESS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * AwdctlBrightnessSkeleton:
 *
 * The #AwdctlBrightnessSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * AwdctlBrightnessSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #AwdctlBrightnessSkeleton.
 */

struct _AwdctlBrightnessSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_awdctl_brightness_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_AWDCTL_BRIGHTNESS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_AWDCTL_BRIGHTNESS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_awdctl_brightness_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_brightness_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_awdctl_brightness_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_brightness_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _awdctl_brightness_skeleton_vtable =
{
  _awdctl_brightness_skeleton_handle_method_call,
  _awdctl_brightness_skeleton_handle_get_property,
  _awdctl_brightness_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
awdctl_brightness_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return awdctl_brightness_interface_info ();
}

static GDBusInterfaceVTable *
awdctl_brightness_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_awdctl_brightness_skeleton_vtable;
}

static GVariant *
awdctl_brightness_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_awdctl_brightness_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _awdctl_brightness_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _awdctl_brightness_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _awdctl_brightness_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "fr.mpostaire.awdctl.Brightness", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _awdctl_brightness_emit_changed (gpointer user_data);

static void
awdctl_brightness_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _awdctl_brightness_emit_changed (skeleton);
}

static void awdctl_brightness_skeleton_iface_init (AwdctlBrightnessIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (AwdctlBrightnessSkeleton, awdctl_brightness_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (AwdctlBrightnessSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_BRIGHTNESS, awdctl_brightness_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (AwdctlBrightnessSkeleton, awdctl_brightness_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_BRIGHTNESS, awdctl_brightness_skeleton_iface_init))

#endif
static void
awdctl_brightness_skeleton_finalize (GObject *object)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (awdctl_brightness_skeleton_parent_class)->finalize (object);
}

static void
awdctl_brightness_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_awdctl_brightness_emit_changed (gpointer user_data)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "fr.mpostaire.awdctl.Brightness",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_awdctl_brightness_schedule_emit_changed (AwdctlBrightnessSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
awdctl_brightness_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _awdctl_brightness_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _awdctl_brightness_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
awdctl_brightness_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _awdctl_brightness_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _awdctl_brightness_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
awdctl_brightness_skeleton_init (AwdctlBrightnessSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = awdctl_brightness_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_AWDCTL_BRIGHTNESS_SKELETON, AwdctlBrightnessSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_UINT);
}

static guint 
awdctl_brightness_skeleton_get_percentage (AwdctlBrightness *object)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
awdctl_brightness_skeleton_get_min_percentage (AwdctlBrightness *object)
{
  AwdctlBrightnessSkeleton *skeleton = AWDCTL_BRIGHTNESS_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
awdctl_brightness_skeleton_class_init (AwdctlBrightnessSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = awdctl_brightness_skeleton_finalize;
  gobject_class->get_property = awdctl_brightness_skeleton_get_property;
  gobject_class->set_property = awdctl_brightness_skeleton_set_property;
  gobject_class->notify       = awdctl_brightness_skeleton_notify;


  awdctl_brightness_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = awdctl_brightness_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = awdctl_brightness_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = awdctl_brightness_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = awdctl_brightness_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (AwdctlBrightnessSkeletonPrivate));
#endif
}

static void
awdctl_brightness_skeleton_iface_init (AwdctlBrightnessIface *iface)
{
  iface->get_percentage = awdctl_brightness_skeleton_get_percentage;
  iface->get_min_percentage = awdctl_brightness_skeleton_get_min_percentage;
}

/**
 * awdctl_brightness_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Brightness.top_of_page">fr.mpostaire.awdctl.Brightness</link>.
 *
 * Returns: (transfer full) (type AwdctlBrightnessSkeleton): The skeleton object.
 */
AwdctlBrightness *
awdctl_brightness_skeleton_new (void)
{
  return AWDCTL_BRIGHTNESS (g_object_new (TYPE_AWDCTL_BRIGHTNESS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface fr.mpostaire.awdctl.Volume
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:AwdctlVolume
 * @title: AwdctlVolume
 * @short_description: Generated C code for the fr.mpostaire.awdctl.Volume D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link> D-Bus interface in C.
 */

/* ---- Introspection data for fr.mpostaire.awdctl.Volume ---- */

static const _ExtendedGDBusArgInfo _awdctl_volume_method_info_set_volume_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _awdctl_volume_method_info_set_volume_IN_ARG_pointers[] =
{
  &_awdctl_volume_method_info_set_volume_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _awdctl_volume_method_info_set_volume =
{
  {
    -1,
    (gchar *) "SetVolume",
    (GDBusArgInfo **) &_awdctl_volume_method_info_set_volume_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-volume",
  FALSE
};

static const _ExtendedGDBusArgInfo _awdctl_volume_method_info_inc_volume_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _awdctl_volume_method_info_inc_volume_IN_ARG_pointers[] =
{
  &_awdctl_volume_method_info_inc_volume_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _awdctl_volume_method_info_inc_volume =
{
  {
    -1,
    (gchar *) "IncVolume",
    (GDBusArgInfo **) &_awdctl_volume_method_info_inc_volume_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-inc-volume",
  FALSE
};

static const _ExtendedGDBusArgInfo _awdctl_volume_method_info_dec_volume_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _awdctl_volume_method_info_dec_volume_IN_ARG_pointers[] =
{
  &_awdctl_volume_method_info_dec_volume_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _awdctl_volume_method_info_dec_volume =
{
  {
    -1,
    (gchar *) "DecVolume",
    (GDBusArgInfo **) &_awdctl_volume_method_info_dec_volume_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-dec-volume",
  FALSE
};

static const _ExtendedGDBusMethodInfo _awdctl_volume_method_info_toggle_volume =
{
  {
    -1,
    (gchar *) "ToggleVolume",
    NULL,
    NULL,
    NULL
  },
  "handle-toggle-volume",
  FALSE
};

static const GDBusMethodInfo * const _awdctl_volume_method_info_pointers[] =
{
  &_awdctl_volume_method_info_set_volume.parent_struct,
  &_awdctl_volume_method_info_inc_volume.parent_struct,
  &_awdctl_volume_method_info_dec_volume.parent_struct,
  &_awdctl_volume_method_info_toggle_volume.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _awdctl_volume_property_info_percentage =
{
  {
    -1,
    (gchar *) "Percentage",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "percentage",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _awdctl_volume_property_info_muted =
{
  {
    -1,
    (gchar *) "Muted",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "muted",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _awdctl_volume_property_info_pointers[] =
{
  &_awdctl_volume_property_info_percentage.parent_struct,
  &_awdctl_volume_property_info_muted.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _awdctl_volume_interface_info =
{
  {
    -1,
    (gchar *) "fr.mpostaire.awdctl.Volume",
    (GDBusMethodInfo **) &_awdctl_volume_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_awdctl_volume_property_info_pointers,
    NULL
  },
  "awdctl-volume",
};


/**
 * awdctl_volume_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
awdctl_volume_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_awdctl_volume_interface_info.parent_struct;
}

/**
 * awdctl_volume_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #AwdctlVolume interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
awdctl_volume_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "percentage");
  g_object_class_override_property (klass, property_id_begin++, "muted");
  return property_id_begin - 1;
}



/**
 * AwdctlVolume:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link>.
 */

/**
 * AwdctlVolumeIface:
 * @parent_iface: The parent interface.
 * @handle_dec_volume: Handler for the #AwdctlVolume::handle-dec-volume signal.
 * @handle_inc_volume: Handler for the #AwdctlVolume::handle-inc-volume signal.
 * @handle_set_volume: Handler for the #AwdctlVolume::handle-set-volume signal.
 * @handle_toggle_volume: Handler for the #AwdctlVolume::handle-toggle-volume signal.
 * @get_muted: Getter for the #AwdctlVolume:muted property.
 * @get_percentage: Getter for the #AwdctlVolume:percentage property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link>.
 */

typedef AwdctlVolumeIface AwdctlVolumeInterface;
G_DEFINE_INTERFACE (AwdctlVolume, awdctl_volume, G_TYPE_OBJECT)

static void
awdctl_volume_default_init (AwdctlVolumeIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * AwdctlVolume::handle-set-volume:
   * @object: A #AwdctlVolume.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.SetVolume">SetVolume()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_volume_complete_set_volume() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-volume",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlVolumeIface, handle_set_volume),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * AwdctlVolume::handle-inc-volume:
   * @object: A #AwdctlVolume.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.IncVolume">IncVolume()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_volume_complete_inc_volume() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-inc-volume",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlVolumeIface, handle_inc_volume),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * AwdctlVolume::handle-dec-volume:
   * @object: A #AwdctlVolume.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.DecVolume">DecVolume()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_volume_complete_dec_volume() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dec-volume",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlVolumeIface, handle_dec_volume),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * AwdctlVolume::handle-toggle-volume:
   * @object: A #AwdctlVolume.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.ToggleVolume">ToggleVolume()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call awdctl_volume_complete_toggle_volume() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-toggle-volume",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AwdctlVolumeIface, handle_toggle_volume),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * AwdctlVolume:percentage:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fr-mpostaire-awdctl-Volume.Percentage">"Percentage"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("percentage", "Percentage", "Percentage", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * AwdctlVolume:muted:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fr-mpostaire-awdctl-Volume.Muted">"Muted"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("muted", "Muted", "Muted", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * awdctl_volume_get_percentage: (skip)
 * @object: A #AwdctlVolume.
 *
 * Gets the value of the <link linkend="gdbus-property-fr-mpostaire-awdctl-Volume.Percentage">"Percentage"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
awdctl_volume_get_percentage (AwdctlVolume *object)
{
  return AWDCTL_VOLUME_GET_IFACE (object)->get_percentage (object);
}

/**
 * awdctl_volume_set_percentage: (skip)
 * @object: A #AwdctlVolume.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fr-mpostaire-awdctl-Volume.Percentage">"Percentage"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
awdctl_volume_set_percentage (AwdctlVolume *object, guint value)
{
  g_object_set (G_OBJECT (object), "percentage", value, NULL);
}

/**
 * awdctl_volume_get_muted: (skip)
 * @object: A #AwdctlVolume.
 *
 * Gets the value of the <link linkend="gdbus-property-fr-mpostaire-awdctl-Volume.Muted">"Muted"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
awdctl_volume_get_muted (AwdctlVolume *object)
{
  return AWDCTL_VOLUME_GET_IFACE (object)->get_muted (object);
}

/**
 * awdctl_volume_set_muted: (skip)
 * @object: A #AwdctlVolume.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fr-mpostaire-awdctl-Volume.Muted">"Muted"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
awdctl_volume_set_muted (AwdctlVolume *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "muted", value, NULL);
}

/**
 * awdctl_volume_call_set_volume:
 * @proxy: A #AwdctlVolumeProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.SetVolume">SetVolume()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_volume_call_set_volume_finish() to get the result of the operation.
 *
 * See awdctl_volume_call_set_volume_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_volume_call_set_volume (
    AwdctlVolume *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetVolume",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_volume_call_set_volume_finish:
 * @proxy: A #AwdctlVolumeProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_volume_call_set_volume().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_volume_call_set_volume().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_set_volume_finish (
    AwdctlVolume *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_set_volume_sync:
 * @proxy: A #AwdctlVolumeProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.SetVolume">SetVolume()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_volume_call_set_volume() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_set_volume_sync (
    AwdctlVolume *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetVolume",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_inc_volume:
 * @proxy: A #AwdctlVolumeProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.IncVolume">IncVolume()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_volume_call_inc_volume_finish() to get the result of the operation.
 *
 * See awdctl_volume_call_inc_volume_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_volume_call_inc_volume (
    AwdctlVolume *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "IncVolume",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_volume_call_inc_volume_finish:
 * @proxy: A #AwdctlVolumeProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_volume_call_inc_volume().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_volume_call_inc_volume().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_inc_volume_finish (
    AwdctlVolume *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_inc_volume_sync:
 * @proxy: A #AwdctlVolumeProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.IncVolume">IncVolume()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_volume_call_inc_volume() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_inc_volume_sync (
    AwdctlVolume *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "IncVolume",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_dec_volume:
 * @proxy: A #AwdctlVolumeProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.DecVolume">DecVolume()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_volume_call_dec_volume_finish() to get the result of the operation.
 *
 * See awdctl_volume_call_dec_volume_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_volume_call_dec_volume (
    AwdctlVolume *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DecVolume",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_volume_call_dec_volume_finish:
 * @proxy: A #AwdctlVolumeProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_volume_call_dec_volume().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_volume_call_dec_volume().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_dec_volume_finish (
    AwdctlVolume *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_dec_volume_sync:
 * @proxy: A #AwdctlVolumeProxy.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.DecVolume">DecVolume()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_volume_call_dec_volume() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_dec_volume_sync (
    AwdctlVolume *proxy,
    guint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DecVolume",
    g_variant_new ("(u)",
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_toggle_volume:
 * @proxy: A #AwdctlVolumeProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.ToggleVolume">ToggleVolume()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_volume_call_toggle_volume_finish() to get the result of the operation.
 *
 * See awdctl_volume_call_toggle_volume_sync() for the synchronous, blocking version of this method.
 */
void
awdctl_volume_call_toggle_volume (
    AwdctlVolume *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ToggleVolume",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * awdctl_volume_call_toggle_volume_finish:
 * @proxy: A #AwdctlVolumeProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_volume_call_toggle_volume().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with awdctl_volume_call_toggle_volume().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_toggle_volume_finish (
    AwdctlVolume *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_call_toggle_volume_sync:
 * @proxy: A #AwdctlVolumeProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.ToggleVolume">ToggleVolume()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See awdctl_volume_call_toggle_volume() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
awdctl_volume_call_toggle_volume_sync (
    AwdctlVolume *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ToggleVolume",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * awdctl_volume_complete_set_volume:
 * @object: A #AwdctlVolume.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.SetVolume">SetVolume()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_volume_complete_set_volume (
    AwdctlVolume *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * awdctl_volume_complete_inc_volume:
 * @object: A #AwdctlVolume.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.IncVolume">IncVolume()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_volume_complete_inc_volume (
    AwdctlVolume *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * awdctl_volume_complete_dec_volume:
 * @object: A #AwdctlVolume.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.DecVolume">DecVolume()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_volume_complete_dec_volume (
    AwdctlVolume *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * awdctl_volume_complete_toggle_volume:
 * @object: A #AwdctlVolume.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fr-mpostaire-awdctl-Volume.ToggleVolume">ToggleVolume()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
awdctl_volume_complete_toggle_volume (
    AwdctlVolume *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * AwdctlVolumeProxy:
 *
 * The #AwdctlVolumeProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * AwdctlVolumeProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #AwdctlVolumeProxy.
 */

struct _AwdctlVolumeProxyPrivate
{
  GData *qdata;
};

static void awdctl_volume_proxy_iface_init (AwdctlVolumeIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (AwdctlVolumeProxy, awdctl_volume_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (AwdctlVolumeProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_VOLUME, awdctl_volume_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (AwdctlVolumeProxy, awdctl_volume_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_VOLUME, awdctl_volume_proxy_iface_init))

#endif
static void
awdctl_volume_proxy_finalize (GObject *object)
{
  AwdctlVolumeProxy *proxy = AWDCTL_VOLUME_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (awdctl_volume_proxy_parent_class)->finalize (object);
}

static void
awdctl_volume_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _awdctl_volume_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
awdctl_volume_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface fr.mpostaire.awdctl.Volume: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
awdctl_volume_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _awdctl_volume_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "fr.mpostaire.awdctl.Volume", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) awdctl_volume_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
awdctl_volume_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_awdctl_volume_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_AWDCTL_VOLUME);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_AWDCTL_VOLUME);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
awdctl_volume_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  AwdctlVolumeProxy *proxy = AWDCTL_VOLUME_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_volume_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_volume_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static guint 
awdctl_volume_proxy_get_percentage (AwdctlVolume *object)
{
  AwdctlVolumeProxy *proxy = AWDCTL_VOLUME_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Percentage");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
awdctl_volume_proxy_get_muted (AwdctlVolume *object)
{
  AwdctlVolumeProxy *proxy = AWDCTL_VOLUME_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Muted");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
awdctl_volume_proxy_init (AwdctlVolumeProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = awdctl_volume_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_AWDCTL_VOLUME_PROXY, AwdctlVolumeProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), awdctl_volume_interface_info ());
}

static void
awdctl_volume_proxy_class_init (AwdctlVolumeProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = awdctl_volume_proxy_finalize;
  gobject_class->get_property = awdctl_volume_proxy_get_property;
  gobject_class->set_property = awdctl_volume_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = awdctl_volume_proxy_g_signal;
  proxy_class->g_properties_changed = awdctl_volume_proxy_g_properties_changed;

  awdctl_volume_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (AwdctlVolumeProxyPrivate));
#endif
}

static void
awdctl_volume_proxy_iface_init (AwdctlVolumeIface *iface)
{
  iface->get_percentage = awdctl_volume_proxy_get_percentage;
  iface->get_muted = awdctl_volume_proxy_get_muted;
}

/**
 * awdctl_volume_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_volume_proxy_new_finish() to get the result of the operation.
 *
 * See awdctl_volume_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
awdctl_volume_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_AWDCTL_VOLUME_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Volume", NULL);
}

/**
 * awdctl_volume_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_volume_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with awdctl_volume_proxy_new().
 *
 * Returns: (transfer full) (type AwdctlVolumeProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlVolume *
awdctl_volume_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return AWDCTL_VOLUME (ret);
  else
    return NULL;
}

/**
 * awdctl_volume_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See awdctl_volume_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type AwdctlVolumeProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlVolume *
awdctl_volume_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_AWDCTL_VOLUME_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Volume", NULL);
  if (ret != NULL)
    return AWDCTL_VOLUME (ret);
  else
    return NULL;
}


/**
 * awdctl_volume_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like awdctl_volume_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call awdctl_volume_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See awdctl_volume_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
awdctl_volume_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_AWDCTL_VOLUME_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Volume", NULL);
}

/**
 * awdctl_volume_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to awdctl_volume_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with awdctl_volume_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type AwdctlVolumeProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlVolume *
awdctl_volume_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return AWDCTL_VOLUME (ret);
  else
    return NULL;
}

/**
 * awdctl_volume_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like awdctl_volume_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See awdctl_volume_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type AwdctlVolumeProxy): The constructed proxy object or %NULL if @error is set.
 */
AwdctlVolume *
awdctl_volume_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_AWDCTL_VOLUME_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "fr.mpostaire.awdctl.Volume", NULL);
  if (ret != NULL)
    return AWDCTL_VOLUME (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * AwdctlVolumeSkeleton:
 *
 * The #AwdctlVolumeSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * AwdctlVolumeSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #AwdctlVolumeSkeleton.
 */

struct _AwdctlVolumeSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_awdctl_volume_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_AWDCTL_VOLUME);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_AWDCTL_VOLUME);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_awdctl_volume_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_volume_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_awdctl_volume_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_awdctl_volume_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _awdctl_volume_skeleton_vtable =
{
  _awdctl_volume_skeleton_handle_method_call,
  _awdctl_volume_skeleton_handle_get_property,
  _awdctl_volume_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
awdctl_volume_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return awdctl_volume_interface_info ();
}

static GDBusInterfaceVTable *
awdctl_volume_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_awdctl_volume_skeleton_vtable;
}

static GVariant *
awdctl_volume_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_awdctl_volume_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _awdctl_volume_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _awdctl_volume_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _awdctl_volume_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "fr.mpostaire.awdctl.Volume", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _awdctl_volume_emit_changed (gpointer user_data);

static void
awdctl_volume_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _awdctl_volume_emit_changed (skeleton);
}

static void awdctl_volume_skeleton_iface_init (AwdctlVolumeIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (AwdctlVolumeSkeleton, awdctl_volume_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (AwdctlVolumeSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_VOLUME, awdctl_volume_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (AwdctlVolumeSkeleton, awdctl_volume_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_AWDCTL_VOLUME, awdctl_volume_skeleton_iface_init))

#endif
static void
awdctl_volume_skeleton_finalize (GObject *object)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (awdctl_volume_skeleton_parent_class)->finalize (object);
}

static void
awdctl_volume_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_awdctl_volume_emit_changed (gpointer user_data)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "fr.mpostaire.awdctl.Volume",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_awdctl_volume_schedule_emit_changed (AwdctlVolumeSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
awdctl_volume_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _awdctl_volume_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _awdctl_volume_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
awdctl_volume_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _awdctl_volume_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _awdctl_volume_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
awdctl_volume_skeleton_init (AwdctlVolumeSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = awdctl_volume_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_AWDCTL_VOLUME_SKELETON, AwdctlVolumeSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
}

static guint 
awdctl_volume_skeleton_get_percentage (AwdctlVolume *object)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
awdctl_volume_skeleton_get_muted (AwdctlVolume *object)
{
  AwdctlVolumeSkeleton *skeleton = AWDCTL_VOLUME_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
awdctl_volume_skeleton_class_init (AwdctlVolumeSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = awdctl_volume_skeleton_finalize;
  gobject_class->get_property = awdctl_volume_skeleton_get_property;
  gobject_class->set_property = awdctl_volume_skeleton_set_property;
  gobject_class->notify       = awdctl_volume_skeleton_notify;


  awdctl_volume_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = awdctl_volume_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = awdctl_volume_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = awdctl_volume_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = awdctl_volume_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (AwdctlVolumeSkeletonPrivate));
#endif
}

static void
awdctl_volume_skeleton_iface_init (AwdctlVolumeIface *iface)
{
  iface->get_percentage = awdctl_volume_skeleton_get_percentage;
  iface->get_muted = awdctl_volume_skeleton_get_muted;
}

/**
 * awdctl_volume_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-fr-mpostaire-awdctl-Volume.top_of_page">fr.mpostaire.awdctl.Volume</link>.
 *
 * Returns: (transfer full) (type AwdctlVolumeSkeleton): The skeleton object.
 */
AwdctlVolume *
awdctl_volume_skeleton_new (void)
{
  return AWDCTL_VOLUME (g_object_new (TYPE_AWDCTL_VOLUME_SKELETON, NULL));
}

